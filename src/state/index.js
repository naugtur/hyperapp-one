const splitState = {
  num: 0,
};

export default {
  one: Object.assign({}, splitState,
    {}, // for component specific state
  ),
  two: Object.assign({}, splitState,
    {}, // for component specific state
  ),
};
